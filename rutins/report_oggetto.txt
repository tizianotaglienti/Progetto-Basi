CREATE PROCEDURE `report_oggetto`(in codice_oggetto varchar(16))
BEGIN
	declare
	
	declare exit handler for sqlexception
	begin
		rollback;
		resignal;
	end;
			
	
		
	# per ogni oggetto devo selezionare lo storico delle offerte
	# tutte le offerte, anche quelle generate dal sistema di controfferta automatica

